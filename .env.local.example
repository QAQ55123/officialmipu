# 複製這個檔案為 .env.local，填入你自己的值

# Supabase 專案設定（在 Supabase 後台 → Project Settings → API 取得）
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 後台管理者相關
# OWNER：最高權限，能看到/操作會員相關工具（合併會員、重設密碼、審核個人頁網址修改）
# 固定的邀請碼，只給你自己或極少數信任的共同經營者
ADMIN_INVITE_CODE_OWNER=請改成一組只有你自己知道的邀請碼

# STAFF：一般管理者，只能管理分類/企劃/商品，看不到會員相關工具
# 不是固定值，改成到後台「Staff 邀請碼管理」區塊按「產生新的邀請碼」，
# 每組邀請碼只能用一次，用過就會失效，需要新增管理者時隨時可以再產生新的

# Session 簽章密鑰：隨便打一串長長的亂數字串即可，不要外流（外流的話有心人可以偽造登入通行證）
ADMIN_TOKEN_SECRET=請改成一串至少32字的隨機亂碼

# Discord Webhook（選填，下單通知用；沒有就留空）
DISCORD_WEBHOOK_URL=

# 寄信服務（用你自己的 Gmail 帳號透過 SMTP 寄信，不需要網域也能寄給任何人）
# 1. 到你的 Gmail 帳號開啟兩步驟驗證
# 2. 到 myaccount.google.com/apppasswords 產生一組應用程式專用密碼（16 碼）
GMAIL_USER=你的Gmail帳號@gmail.com
GMAIL_APP_PASSWORD=你產生的16碼應用程式密碼
EMAIL_FROM=米舖 <你的Gmail帳號@gmail.com>

# 你的網站正式網址（寄信裡的連結會用到），例如 https://mibu-app.vercel.app
# 本機測試不用填，會自動用 http://localhost:3000
NEXT_PUBLIC_SITE_URL=

# Google Sheet 資料同步（選填，不設定的話這個功能就是靜靜跳過，不影響其他功能）
# 設定方式：
# 1. 到 console.cloud.google.com 建立專案，啟用 Google Sheets API
# 2. 建立服務帳戶，下載 JSON 金鑰
# 3. 建一個 Google Sheet，把金鑰裡的 client_email 加進「共用」，給編輯者權限
# 4. 複製 Sheet 網址上的試算表 ID，填到下面
GOOGLE_SERVICE_ACCOUNT_EMAIL=你的服務帳戶信箱@xxx.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n貼上金鑰JSON裡的private_key欄位\n-----END PRIVATE KEY-----\n"
# ↑ 這個雙引號寫法只適用在 .env.local 這種「檔案」裡。
# 如果是在 Vercel 網頁後台的環境變數欄位貼，不要加前後的雙引號，直接貼金鑰本身就好
# （欄位本身就是純文字輸入框，雙引號會被當成金鑰內容的一部分，導致格式錯誤解析失敗）
GOOGLE_SHEET_ID=你的Google Sheet網址中間那一段ID

# 成本表用的是「另一份獨立的試算表」（跟訂單/會員/企劃/商品那份分開），
# 一樣要把同一個服務帳戶 client_email 加進「共用」給編輯者權限，
# 訂單編號/帳號/企劃/金額會自動同步過去，「成本」欄留空給你自己填，
# 「利潤」欄是公式（=金額-成本）會自動算，已經同步過的訂單不會被覆蓋、不會洗掉你填的成本。
# 沒有要用成本表的話，這個變數留空不填就好，不影響其他功能。
GOOGLE_COST_SHEET_ID=你的成本試算表網址中間那一段ID
